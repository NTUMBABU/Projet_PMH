import 'package:flutter/material.dart';

class MapD extends StatelessWidget {
  const MapD({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Icon(Icons.alarm,size: 64.0,color: Color.fromARGB(255, 89, 64, 255),),
      );
  }
}