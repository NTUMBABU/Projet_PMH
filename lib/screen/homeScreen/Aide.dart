import 'package:flutter/material.dart';

class Aide extends StatelessWidget {
  const Aide({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Icon(Icons.cloud,size: 64.0,color: Colors.amberAccent,),
    );
  }
}